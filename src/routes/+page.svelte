<script lang="ts">
	import { goto } from '$app/navigation';
	import { resolve } from '$app/paths';
	import { onMount } from 'svelte';

	onMount(() => {
		goto(resolve('/dashboard'));
	});
</script>

<svelte:head>
	<title>Darpan — School Management</title>
</svelte:head>
